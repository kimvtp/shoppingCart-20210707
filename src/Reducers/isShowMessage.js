
const defaultState = false;
let isShowMessage = (state = defaultState, action) => {
    switch (action.type){
        default:
            return state;
        
    }
}

export default isShowMessage;
